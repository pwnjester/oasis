#ifndef WINDOW_H
#define WINDOW_H

// This is where the window is created, and the main loop is started
int begin_ui_window(int width, int height, const char *title,
                    unsigned int flags);

#endif
